# Server-side only — never expose DATABASE_URL to the client bundle.
# Set this in your Vercel project environment variables (Production + Preview),
# not in a committed .env file.
DATABASE_URL=

# Set to 'true' only after IRB approval has confirmed the storage location.
# Default is false — the app runs normally and writes nothing to the database.
VITE_RECORDING_ENABLED=false
